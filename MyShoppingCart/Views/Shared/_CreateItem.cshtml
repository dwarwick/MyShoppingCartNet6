@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole("Seller"))
{
    <div style="position: fixed; right: 25px; bottom: 70px;" class="text-white">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-success" style="display: inline-block;height:100%;width:100%">
            <i class="bi bi-plus-circle"></i> Add New
        </a>
    </div>
}
